<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seeyon.orgcenter.manage.role.mapper.OrgPermissionFunRelationMapper">
	<!--    查询账户byroleid-->
	<select id="getFunctionByRoleID" resultType="com.seeyon.orgcenter.manage.function.entity.OrgFunction">
        select * from org_function ofu where ofu.id exists
        (select oar.fun_id  from org_permission_fun_relation oar where oar.app_id=#{AppId} and oar.role_id=#{RoleID})
	</select>

	<!--查询角色byfunid-->
	<select id="getRolesByFunID" resultType="com.seeyon.orgcenter.manage.role.entity.OrgRole">
         select * from org_role oro where oro.id exists
                (select oar.role_id  from org_permission_fun_relation oar where oar.app_id=#{AppId} and oar.fun_id=#{FunID})
    </select>
</mapper>
